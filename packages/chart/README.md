# Chart

An Chart for React App.

## Install

    yarn add @millifx/chart

## Usage

    import { ErrorBoundary } from '@millifx/chart';

    const App = () => (
      <Chart data={data} />
    );    
